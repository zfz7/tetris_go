{
  "devDependencies": {
    "@testing-library/cypress": "^8.0.3",
    "@types/testing-library__cypress": "^5.0.9",
    "cypress": "^10.7.0",
    "start-server-and-test": "^1.14.0",
    "typescript": "^4.8.2"
  },
  "scripts": {
    "open": "start-server-and-test 'cd .. && SERVER_PORT=8444 ./start.sh' tcp:8444 cypress-open",
    "cypress-run": "cypress run",
    "cypress-open": "cypress open",
    "test": "start-server-and-test 'cd .. && SERVER_PORT=8444 ./start.sh' tcp:8444 cypress-run"
  }
}
